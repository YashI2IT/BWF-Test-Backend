# Excel Folder Policy

Store all spreadsheet workflows under this directory.

- `incoming/`: raw uploaded files, unchanged
- `processed/`: normalized sheets used by apps/services
- `archive/`: previous versions grouped by date
- `templates/`: canonical template files
- `exports/`: generated output sheets

Recommended naming:

`YYYY-MM-DD_<home-or-module>_<purpose>.xlsx`
